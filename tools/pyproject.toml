[project]
name = "rapidai-tools"
version = "0.1.0"
description = "rapidAI Phase 0 experiment tooling"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["rapidai_tools"]
