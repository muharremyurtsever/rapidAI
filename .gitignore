# Large artifacts — never committed (root-level only; docs/experiments/data IS committed)
/models/
/data/
*.gguf
*.safetensors
*.rapd

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# C++ / CMake
build/
cmake-build-*/
compile_commands.json

# macOS
.DS_Store

# Editors
.idea/
.vscode/

# Native extension binaries (rebuild via tools/native)
*.so

# paper build artifacts
docs/paper/*.zip
